schema_version: "1.0"

name: "@gcornut/optimize-svg-path-string-declaration"
version: "1.0.7"
description: "Optimize SVG path in string literal"
author: "gcornut <gcornut@users.noreply.github.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
    languages: ["typescript"]

keywords: ["transformation", "migration"]

registry:
    access: "public"
    visibility: "public"
