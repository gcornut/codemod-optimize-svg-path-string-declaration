schema_version: "1.0"

name: "optimize-svg-path-string-declaration"
version: "0.1.0"
description: "Optimize SVG path in string literal"
author: "Author <author@example.com>"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
    languages: ["typescript"]

keywords: ["transformation", "migration"]

registry:
    access: "public"
    visibility: "public"
